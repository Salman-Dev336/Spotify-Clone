const mongoose = require("mongoose");


async function connectDB(){
    try {
        
    } catch (error) {
        console.log("database connection failed", error);
        process.exit(1);
        
    }
}